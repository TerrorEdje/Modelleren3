using MensErgerJe.View_Model;
using System.Windows.Input;

namespace MensErgerJe.Commands
{
    //internal class ThrowDice : ICommand
    //{
        /*private BoardView_Model viewModel;

        public ThrowDice(BoardView_Model vm) 
        {
            viewModel = vm;
        }*/

        /*
        event System.EventHandler ICommand.CanExecuteChanged
        {
            add { CommandManager.RequerySuggested += value; }
            remove { CommandManager.RequerySuggested -= value; }
        }

        bool ICommand.CanExecute(object parameter)
        {
            return viewModel.CanThrow;
        }

        
        void ICommand.Execute(object parameter)
        {
            viewModel.SaveChanges;
        }*/
    //}
}
